/**
 * Package card (feature alias).
 *
 * The canonical implementation lives at `shared/components/cards/PackageCard`.
 * Re-exported here so feature imports can use the shorter path.
 */
export { PackageCard } from '../../shared/components/cards';
export type { PackageCardProps, PackageCardVariant } from '../../shared/components/cards';
