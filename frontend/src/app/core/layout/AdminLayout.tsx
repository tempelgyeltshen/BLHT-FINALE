/**
 * Admin layout (core alias).
 *
 * The full implementation lives at `features/admin/components/shared/AdminLayout`
 * — this file re-exports it so the core layout path stays available without
 * duplicating the sidebar/topbar markup.
 */
export { AdminLayout } from '../../features/admin/components/shared/AdminLayout';
export type { AdminLayoutProps } from '../../features/admin/components/shared/AdminLayout';
