/**
 * Public layout (alias).
 *
 * The public chrome (Navbar + animated outlet + Footer) lives in `MainLayout`.
 * This file exposes it under the `PublicLayout` name for route tables that
 * prefer the semantic name.
 */
export { MainLayout as PublicLayout } from './MainLayout';
